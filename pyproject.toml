[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "anne-core"
version = "0.2.0"
description = "ANNE Core — Open Cognitive Architecture with local private knowledge and distributed knowledge primitives."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Mustafa Gökhan Yılmaz", email = "mgy421977@gmail.com"}
]
keywords = ["cognitive-architecture", "ai-orchestration", "cognitive-memory", "distributed-knowledge", "privacy-by-design"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "click>=8.1.0",
    "cryptography>=43.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
]
openai = [
    "openai>=1.0.0",
]
xai = [
    "openai>=1.0.0",
]

[project.scripts]
anne-core = "anne_core.cli:main"

[project.urls]
Homepage = "https://github.com/mgy421977-bit/anne-core"
Repository = "https://github.com/mgy421977-bit/anne-core"
Issues = "https://github.com/mgy421977-bit/anne-core/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py311"
